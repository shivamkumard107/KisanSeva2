<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.uttarakhand.kisanseva2">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:fullBackupContent="@xml/backup_descriptor"
        android:icon="@drawable/app_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/app_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.TreatmentDetails"></activity>
        <activity android:name=".activities.CropDetailActivity" />
        <activity android:name=".activities.WebActivity" />
        <activity android:name=".activities.SelectPolicyActivity" />
        <activity android:name=".activities.HorticultureDetailActivity" />
        <activity android:name=".activities.HorticultureActivity" />
        <activity android:name=".activities.SoilHealthActivity" />
        <activity android:name=".activities.SelectProblemActivity" />
        <activity android:name=".activities.CropProductionActivity" />
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>